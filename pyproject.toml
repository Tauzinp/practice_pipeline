[project]
name = "practice-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bio>=1.8.1",
    "ipython>=9.10.0",
    "plotext>=5.3.2",
    "python-dotenv>=1.2.1",
]
